## CodeBook 

This code perform this process:

* Reads all necesary files.
* Gets only Mean and Std variables.
* Join Test and Train sets on a tidy set.
* Writes the result on a txt file (Tidy.txt).
* Create a independet tidy set with the average of each variable.
* Writes the result on a txt file (Tidy_Average.txt).
